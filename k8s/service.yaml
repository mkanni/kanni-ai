apiVersion: v1
kind: Service
metadata:
  name: kanni-ai-service
  namespace: kanni-ai
  labels:
    app: kanni-ai
spec:
  selector:
    app: kanni-ai
  ports:
  - name: http
    port: 80
    targetPort: 80
  type: ClusterIP